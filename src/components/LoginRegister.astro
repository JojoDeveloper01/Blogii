---

---

<style>
    button {
        background: var(--primary); /* Gold for buttons */
        color: black; /* Text color on buttons */
        font-size: 0.9vw;
    }

    button:hover {
        background: var(--primary); /* Slightly darker gold for hover effect */
    }

    /*   input {
        background: rgba(255, 255, 255, 0.1);
        color: white; 
        border: 1px solid rgba(255, 255, 255, 0.3); 
    } */

    input::placeholder {
        color: rgba(255, 255, 255, 0.5); /* Placeholder visibility */
    }
</style>

<!-- Modal de Login -->
<dialog
    id="modalLogin"
    class="backdrop:bg-black/50 blurred-background w-full max-w-md p-0 rounded-lg shadow-lg"
>
</dialog>

<!-- Modal de Registro -->
<dialog
    id="modalRegister"
    class="backdrop:bg-black/50 blurred-background w-full max-w-md p-0 rounded-lg shadow-lg"
>
</dialog>
